#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    """Update the dictionary with the key/value pair or add it if it doesn't exist."""
    a_dictionary[key] = value
    return a_dictionary
